# Infinity Kernel v1.0.49
# Poco X3 Pro (vayu/bhima) | SM8150 (LineageOS base) | Linux 4.14.357
# LLVM/Clang 23 (apt.llvm.org) | AnyKernel3 | MIT License
#
# v1.0.49 — Source compat fixes rewritten:
#   All 7 fixes now use pure sed/awk (NO Python heredocs).
#   Fixes Python IndentationError from v1.0.48 Fix 5/7.
#   Base: LineageOS/android_kernel_qcom_sm8150
#   CLANG_TRIPLE=aarch64-linux-gnu- (native Makefile support, no wrapper)
version: 2.1

jobs:
  build:
    docker:
      - image: ubuntu:22.04
    steps:
      - checkout

      # ── Step 1: Install dependencies + Clang 23 ──────────────
      - run:
          name: "Install dependencies + LLVM/Clang 23"
          command: |
            apt-get update
            apt-get install -y \
              bc bison build-essential ccache curl flex \
              git gnupg gperf imagemagick \
              libelf-dev liblz4-tool libncurses5-dev \
              libssl-dev libxml2 libxml2-utils lzop \
              rsync schedtool squashfs-tools software-properties-common \
              xsltproc zip zlib1g-dev \
              gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu \
              gcc-arm-linux-gnueabi binutils-arm-linux-gnueabi \
              python-is-python3 ca-certificates lsb-release wget

            wget -qO- https://apt.llvm.org/llvm.sh | bash -s 23

            CLANG_BIN="/usr/lib/llvm-23/bin/clang"
            echo "=== Clang 23 ==="
            echo "clang:   $($CLANG_BIN --version 2>&1 | head -1)"
            echo "ld.lld:  $(ls -la /usr/lib/llvm-23/bin/ld.lld)"
            echo "llvm-ar: $(ls -la /usr/lib/llvm-23/bin/llvm-ar)"
            echo "aarch64: $(which aarch64-linux-gnu-gcc)"
            echo "arm:     $(which arm-linux-gnueabi-gcc)"

      # ── Step 2: Clone LineageOS kernel source ───────────────
      - run:
          name: "Clone LineageOS android_kernel_qcom_sm8150"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            if [ ! -d "$KERNEL_SRC/.git" ]; then
              git clone --depth=1 \
                https://github.com/LineageOS/android_kernel_qcom_sm8150 \
                "$KERNEL_SRC"
            fi
            echo "KERNEL_SRC=$KERNEL_SRC"
            echo "SRC_ROOT=${SRC_ROOT:-.}"

      # ── Step 3: KernelSU-Next v3.2.0 ────────────────────────
      - run:
          name: "KernelSU-Next v3.2.0 setup"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cd "$KERNEL_SRC"
            if [ -d "KernelSU/kernel" ]; then
              echo "KernelSU-Next already present"
            else
              KSU_CLONED=0
              for KSU_URL in \
                "https://github.com/KernelSU-Next/KernelSU" \
                "https://github.com/negrroo/KernelSU" \
                "https://github.com/tiann/KernelSU"; do
                echo "Trying: $KSU_URL"
                if GIT_TERMINAL_PROMPT=0 git clone --depth=1 --branch main \
                     --config "credential.helper=" \
                     "$KSU_URL" KernelSU 2>/dev/null; then
                  KSU_CLONED=1
                  echo "Cloned from: $KSU_URL"
                  break
                fi
                rm -rf KernelSU
              done
              if [ "$KSU_CLONED" = "0" ]; then
                for KSU_URL in \
                  "https://github.com/KernelSU-Next/KernelSU" \
                  "https://github.com/negrroo/KernelSU" \
                  "https://github.com/tiann/KernelSU"; do
                  if GIT_TERMINAL_PROMPT=0 git clone --depth=1 --branch v3.2.0 \
                       --config "credential.helper=" \
                       "$KSU_URL" KernelSU 2>/dev/null; then
                    KSU_CLONED=1
                    echo "Cloned from: $KSU_URL (branch v3.2.0)"
                    break
                  fi
                  rm -rf KernelSU
                done
              fi
              if [ "$KSU_CLONED" = "1" ] && [ -f KernelSU/kernel/setup.sh ]; then
                bash KernelSU/kernel/setup.sh -o KernelSU 2>/dev/null || true
              fi
              if [ "$KSU_CLONED" = "1" ]; then
                echo "KernelSU ready"
              else
                echo "WARNING: KernelSU not cloned — building without it"
              fi
            fi

      # ── Step 4: SuSFS v2.1.0 ────────────────────────────────
      - run:
          name: "SuSFS v2.1.0 patch (Linux 4.14)"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cd "$KERNEL_SRC"
            SUSFS_MARKER=".susfs_v2.1.0_applied"
            if [ -f "$SUSFS_MARKER" ]; then
              echo "SuSFS v2.1.0 already applied"
            else
              PATCH_FOUND=0
              for repo_url in \
                "https://github.com/AnymoreProject/susfs4ksu" \
                "https://github.com/sidex15/susfs4ksu"; do
                tmp_repo=$(mktemp -d)
                if GIT_TERMINAL_PROMPT=0 git clone --depth=1 \
                     --config "credential.helper=" \
                     "$repo_url" "$tmp_repo/repo" 2>/dev/null; then
                  PATCH_FILE=$(find "$tmp_repo/repo" -name "*4.14*.patch" 2>/dev/null | sort | head -1)
                  if [ -n "$PATCH_FILE" ] && [ -f "$PATCH_FILE" ]; then
                    echo "Found patch: $(basename "$PATCH_FILE")"
                    if grep -q "susfs" "$PATCH_FILE" 2>/dev/null; then
                      patch -p1 --forward < "$PATCH_FILE" 2>/dev/null || true
                      PATCH_FOUND=1
                      touch "$SUSFS_MARKER"
                    fi
                  fi
                fi
                rm -rf "$tmp_repo"
                [ "$PATCH_FOUND" = "1" ] && break
              done
              if [ "$PATCH_FOUND" = "0" ]; then
                echo "WARNING: SuSFS patch not found automatically"
              fi
            fi

      # ── Step 5: Copy infinity files ──────────────────────────
      - run:
          name: "Copy infinity kernel files"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cp -rv "$SRC_ROOT/arch/arm64/configs/infinity_defconfig" \
                    "$KERNEL_SRC/arch/arm64/configs/infinity_defconfig"
            if [ -d "$SRC_ROOT/drivers/charging" ]; then
              cp -rv "$SRC_ROOT/drivers/charging" "$KERNEL_SRC/drivers/charging"
            fi
            if ! grep -q 'source "drivers/charging/Kconfig"' "$KERNEL_SRC/drivers/Kconfig" 2>/dev/null; then
              echo 'source "drivers/charging/Kconfig"' >> "$KERNEL_SRC/drivers/Kconfig"
            fi
            if ! grep -q 'obj-$(CONFIG_CHARGING_CONTROL)' "$KERNEL_SRC/drivers/Makefile" 2>/dev/null; then
              echo 'obj-$(CONFIG_CHARGING_CONTROL)      += charging/' >> "$KERNEL_SRC/drivers/Makefile"
            fi
            if [ -d "$SRC_ROOT/patches" ]; then
              cp -rv "$SRC_ROOT/patches" "$KERNEL_SRC/patches"
            fi
            echo "Files copied."

      # ── Step 6: Config (defconfig + merge + triple disable + HIDDEN_CFGS) ──
      - run:
          name: "Config (defconfig + merge + triple disable + HIDDEN_CFGS)"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cd "$KERNEL_SRC"

            PROTECTED_CFGS="CONFIG_NO_HZ_COMMON CONFIG_NO_HZ_IDLE CONFIG_VIRT_CPU_ACCOUNTING_GEN CONFIG_CONTEXT_TRACKING CONFIG_NO_HZ CONFIG_HZ_PERIODIC CONFIG_NO_HZ_FULL CONFIG_CPUSETS"
            DISABLE_LIST="CONFIG_STAGING CONFIG_SOUND CONFIG_SND CONFIG_RC_CORE CONFIG_MEDIA_SUPPORT CONFIG_FB CONFIG_VT CONFIG_VHOST CONFIG_COMEDI CONFIG_AGP CONFIG_INFINIBAND"

            make ARCH=arm64 vendor/sm8150_defconfig O=out
            scripts/kconfig/merge_config.sh -O out/ out/.config \
              arch/arm64/configs/infinity_defconfig || true
            make ARCH=arm64 olddefconfig O=out </dev/null || true

            disable_cfgs() {
              for cfg in $DISABLE_LIST; do
                local skip=0
                for p in $PROTECTED_CFGS; do
                  if [ "$cfg" = "$p" ]; then skip=1; break; fi
                done
                [ "$skip" = "1" ] && continue
                sed -i "s/^${cfg}=y/# ${cfg} is not set/" out/.config
                sed -i "s/^${cfg}=m/# ${cfg} is not set/" out/.config
              done
              sed -i 's/^CONFIG_CC_STACKPROTECTOR=y/# CONFIG_CC_STACKPROTECTOR is not set/' out/.config
              sed -i 's/^CONFIG_CC_STACKPROTECTOR_STRONG=y/# CONFIG_CC_STACKPROTECTOR_STRONG is not set/' out/.config
              sed -i '/^CONFIG_CC_STACKPROTECTOR_NONE/d' out/.config
              echo "CONFIG_CC_STACKPROTECTOR_NONE=y" >> out/.config
            }

            echo "  [1/6] First disable pass..."
            disable_cfgs
            make ARCH=arm64 olddefconfig O=out </dev/null || true

            echo "  [2/6] Second disable pass..."
            disable_cfgs
            make ARCH=arm64 olddefconfig O=out </dev/null || true

            echo "  [3/6] Third disable pass (sed kill)..."
            _skip=0
            for cfg in $DISABLE_LIST; do
              _skip=0
              for p in $PROTECTED_CFGS; do
                if [ "$cfg" = "$p" ]; then _skip=1; break; fi
              done
              [ "$_skip" = "1" ] && continue
              sed -i "/^${cfg}=/d" out/.config
              if ! grep -q "^# ${cfg} is not set" out/.config; then
                echo "# ${cfg} is not set" >> out/.config
              fi
            done
            make ARCH=arm64 olddefconfig O=out </dev/null || true

            HIDDEN_CFGS="CONFIG_SCHED_INFO CONFIG_PINCTRL CONFIG_POSIX_TIMERS CONFIG_RTC_CLASS CONFIG_SCHED_WALT CONFIG_DEBUG_REGULATOR"

            echo "  [4/6] Compiling scripts..."
            make ARCH=arm64 O=out -j1 scripts 2>&1 | tail -5 || true

            echo "  [5/6] Patching HIDDEN_CFGS..."
            for cfg in $HIDDEN_CFGS; do
              if ! grep -q "^${cfg}=y" out/.config; then
                sed -i "/^# ${cfg} is not set/d" out/.config
                echo "${cfg}=y" >> out/.config
              fi
              if [ -f out/include/config/auto.conf ]; then
                if ! grep -q "^${cfg}=y" out/include/config/auto.conf; then
                  sed -i "/^${cfg}=/d" out/include/config/auto.conf
                  echo "${cfg}=y" >> out/include/config/auto.conf
                fi
              fi
              if [ -f out/include/generated/autoconf.h ]; then
                if ! grep -q "#define ${cfg} 1" out/include/generated/autoconf.h; then
                  sed -i "/#define ${cfg} /d" out/include/generated/autoconf.h
                  echo "#define ${cfg} 1" >> out/include/generated/autoconf.h
                fi
              fi
            done

            echo "  [6/6] Verifying..."
            for cfg in $HIDDEN_CFGS; do
              grep -q "^${cfg}=y" out/.config 2>/dev/null && echo "  OK:   ${cfg}" || echo "  FAIL: ${cfg} NOT in .config!"
            done
            touch out/include/config/auto.conf out/include/generated/autoconf.h 2>/dev/null || true
            echo "Config ready."

      # ── Step 7: OpenSSL 3.0 patches ──────────────────────────
      - run:
          name: "OpenSSL 3.0 patches"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            if [ -f "$KERNEL_SRC/certs/extract-cert.c" ]; then
              sed -i '1i #define OPENSSL_SUPPRESS_DEPRECATED 1' "$KERNEL_SRC/certs/extract-cert.c"
            fi
            if [ -f "$KERNEL_SRC/scripts/sign-file.c" ]; then
              sed -i '1i #define OPENSSL_SUPPRESS_DEPRECATED 1' "$KERNEL_SRC/scripts/sign-file.c"
            fi
            echo "OpenSSL patches applied."

      # ── Step 8: apply_all.sh + SuSFS config ──────────────────
      - run:
          name: "Apply patches + SuSFS config"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cd "$KERNEL_SRC"
            if [ -f patches/apply_all.sh ]; then
              bash patches/apply_all.sh .
            fi
            if grep -q "CONFIG_SUFS_FS" out/.config 2>/dev/null; then
              echo "SUFS_FS already in .config"
            else
              if find . -name "Kconfig.sufs" -o -name "Kconfig.susfs" 2>/dev/null | head -1 | grep -q .; then
                echo "CONFIG_SUFS_FS=y" >> out/.config
                echo "CONFIG_SUFS=y" >> out/.config
              fi
            fi
            make ARCH=arm64 olddefconfig O=out </dev/null || true
            echo "Patches applied."

      # ── Step 9: Source compatibility fixes (7 fixes, ALL sed) ──
      - run:
          name: "Source compatibility fixes (7 fixes, pure sed)"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cd "$KERNEL_SRC"
            set -o pipefail

            echo "=== Fix 1/7: filter.h compat_uptr_t ==="
            # Wrap struct compat_sock_fprog in #ifdef CONFIG_COMPAT
            # Use sed: find the struct definition and add guards
            if [ -f "net/core/filter.c" ]; then
              # Write a temp awk script to avoid Python heredoc issues
              cat > /tmp/fix_filter.awk << 'AWKEOF'
              BEGIN { in_struct = 0; added = 0 }
              /struct compat_sock_fprog/ {
                if (!added) {
                  print "#ifdef CONFIG_COMPAT"
                  added = 1
                }
                in_struct = 1
              }
              in_struct && /\};/ {
                print $0
                print "#endif /* CONFIG_COMPAT */"
                in_struct = 0
                next
              }
              { print $0 }
AWKEOF
              awk -f /tmp/fix_filter.awk net/core/filter.c > /tmp/filter.c.fixed
              mv /tmp/filter.c.fixed net/core/filter.c
              echo "  Done (filter.c)"
            fi

            echo "=== Fix 2/7: hugetlbpage.c ptep rename ==="
            if [ -f "mm/hugetlbpage.c" ]; then
              sed -i 's/ptep = huge_pmd_share/pte = huge_pmd_share/' mm/hugetlbpage.c
              echo "  Done"
            fi

            echo "=== Fix 3/7: huge_memory.c try_to_unmap arg ==="
            if [ -f "mm/huge_memory.c" ]; then
              sed -i 's/try_to_unmap(page, ttu_flags);/try_to_unmap(page, ttu_flags, NULL);/' mm/huge_memory.c
              echo "  Done"
            fi

            echo "=== Fix 4/7: khugepaged.c nr_ptes removal ==="
            if [ -f "mm/khugepaged.c" ]; then
              sed -i '/atomic_long_dec(&mm->nr_ptes)/d' mm/khugepaged.c
              echo "  Done"
            fi

            echo "=== Fix 5/7: sock.c compat_timeval guard ==="
            # Define compat_timeval struct when CONFIG_COMPAT is not set.
            # On arm64: compat_timeval = { compat_long_t tv_sec, tv_usec }
            # compat_long_t = s32 on arm64. Using int is safe (same size).
            if [ -f "net/core/sock.c" ] && grep -q 'compat_timeval' net/core/sock.c; then
              # Write guard to temp file (avoids sed escape issues)
              cat > /tmp/compat_guard.h << 'GUARDEOF'
#if !defined(CONFIG_COMPAT)
struct compat_timeval {
        int tv_sec;
        int tv_usec;
};
#endif
GUARDEOF
              # Insert after last #include line
              LAST_INC=$(grep -n '^#include' net/core/sock.c | tail -1 | cut -d: -f1)
              if [ -n "$LAST_INC" ]; then
                sed -i "${LAST_INC}r /tmp/compat_guard.h" net/core/sock.c
              fi
              rm -f /tmp/compat_guard.h
              echo "  Done (sock.c compat_timeval guard added)"
            else
              echo "  Skipped (no compat_timeval or file missing)"
            fi

            echo "=== Fix 6/7: net/compat.c conditional compile ==="
            # If CONFIG_COMPAT is disabled, compat.c may reference
            # undefined symbols. Guard the entire file or stub it.
            if [ -f "net/compat.c" ] && ! grep -q 'CONFIG_COMPAT' net/compat.c; then
              # Add CONFIG_COMPAT guard at top
              sed -i '1i\#ifdef CONFIG_COMPAT' net/compat.c
              echo '#endif /* CONFIG_COMPAT */' >> net/compat.c
              echo "  Done (net/compat.c wrapped)"
            fi

            echo "=== Fix 7/7: fs/compat.c conditional compile ==="
            if [ -f "fs/compat.c" ] && ! grep -q '^#ifdef CONFIG_COMPAT' fs/compat.c; then
              sed -i '1i\#ifdef CONFIG_COMPAT' fs/compat.c
              echo '#endif /* CONFIG_COMPAT */' >> fs/compat.c
              echo "  Done (fs/compat.c wrapped)"
            fi

            echo "=== All 7 fixes applied ==="

      # ── Step 10: Clean source tree ───────────────────────────
      - run:
          name: "Clean source tree"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            cd "$KERNEL_SRC"
            rm -f .config
            rm -rf include/config
            echo "Source tree cleaned."

      # ── Step 11: Build kernel ────────────────────────────────
      - run:
          name: "Build Infinity Kernel (Clang 23 + KSU-Next + SuSFS)"
          no_output_timeout: 30m
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            CLANG_DIR="/usr/lib/llvm-23/bin"
            cd "$KERNEL_SRC"

            # Regenerate config after source fixes
            make ARCH=arm64 O=out olddefconfig </dev/null 2>&1 || true

            # Re-apply HIDDEN_CFGS after olddefconfig
            HIDDEN_CFGS="CONFIG_SCHED_INFO CONFIG_PINCTRL CONFIG_POSIX_TIMERS CONFIG_RTC_CLASS CONFIG_SCHED_WALT CONFIG_DEBUG_REGULATOR"
            for cfg in $HIDDEN_CFGS; do
              if ! grep -q "^${cfg}=y" out/.config; then
                sed -i "/^# ${cfg} is not set/d" out/.config
                echo "${cfg}=y" >> out/.config
              fi
              if [ -f out/include/config/auto.conf ]; then
                if ! grep -q "^${cfg}=y" out/include/config/auto.conf; then
                  sed -i "/^${cfg}=/d" out/include/config/auto.conf
                  echo "${cfg}=y" >> out/include/config/auto.conf
                fi
              fi
              if [ -f out/include/generated/autoconf.h ]; then
                if ! grep -q "#define ${cfg} 1" out/include/generated/autoconf.h; then
                  sed -i "/#define ${cfg} /d" out/include/generated/autoconf.h
                  echo "#define ${cfg} 1" >> out/include/generated/autoconf.h
                fi
              fi
            done
            touch out/include/config/auto.conf out/include/generated/autoconf.h 2>/dev/null || true

            set -o pipefail

            # ── Build: CC=clang CLANG_TRIPLE=aarch64-linux-gnu- (NO wrapper) ──
            echo "=== Building kernel ==="
            make O=out \
              ARCH=arm64 \
              CC=$CLANG_DIR/clang \
              CLANG_TRIPLE=aarch64-linux-gnu- \
              CROSS_COMPILE=aarch64-linux-gnu- \
              CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
              LD=$CLANG_DIR/ld.lld \
              AR=$CLANG_DIR/llvm-ar \
              NM=$CLANG_DIR/llvm-nm \
              OBJCOPY=$CLANG_DIR/llvm-objcopy \
              OBJDUMP=$CLANG_DIR/llvm-objdump \
              STRIP=$CLANG_DIR/llvm-strip \
              HOSTCC=gcc \
              HOSTCFLAGS="-Wall -Wmissing-prototypes -Wstrict-prototypes -O2 -fomit-frame-pointer -std=gnu89 -Wno-error" \
              KCFLAGS="-Wno-error" \
              -j$(nproc) Image.gz-dtb dtbs 2>&1 | tee /tmp/kernel_build.log

            if [ ${PIPESTATUS[0]} -ne 0 ]; then
              echo ""
              echo "=========================================="
              echo "  BUILD ERROR"
              echo "=========================================="
              grep -E "error:" /tmp/kernel_build.log | head -40
              echo "--- Last 80 lines ---"
              tail -80 /tmp/kernel_build.log
              exit 1
            fi

      # ── Step 12: Package AnyKernel3 ZIP ──────────────────────
      - run:
          name: "Package AnyKernel3 ZIP"
          command: |
            KERNEL_SRC="/tmp/infinity-kernel-build/kernel_src"
            IMG="$KERNEL_SRC/out/arch/arm64/boot/Image.gz-dtb"

            if [ ! -f "$IMG" ]; then
              echo "ERROR: Image.gz-dtb not found!"
              ls -la "$KERNEL_SRC/out/arch/arm64/boot/" 2>/dev/null || echo "boot dir missing"
              exit 1
            fi

            echo "Image.gz-dtb: $(du -sh "$IMG" | cut -f1)"

            if [ -d "$SRC_ROOT/AnyKernel3" ]; then
              cp -v "$IMG" "$SRC_ROOT/AnyKernel3/"
              cd "$SRC_ROOT/AnyKernel3"
              zip -r9 /tmp/infinity-kernel-v1.0.49.zip *
              echo "ZIP: /tmp/infinity-kernel-v1.0.49.zip ($(du -sh /tmp/infinity-kernel-v1.0.49.zip | cut -f1))"
            else
              echo "ERROR: AnyKernel3 directory not found"
              exit 1
            fi

      - store_artifacts:
          path: /tmp/infinity-kernel-v1.0.49.zip
          destination: infinity-kernel-v1.0.49.zip

workflows:
  infinity_build:
    jobs:
      - build