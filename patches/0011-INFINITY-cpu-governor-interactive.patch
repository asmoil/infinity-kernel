diff --git a/drivers/cpufreq/Kconfig b/drivers/cpufreq/Kconfig
--- a/drivers/cpufreq/Kconfig
+++ b/drivers/cpufreq/Kconfig
@@ -121,7 +121,13 @@
 	  loading your cpufreq low-level hardware driver, using the
 	  'interactive' governor for latency-sensitive workloads.
 
 endchoice
+config INFINITY_CPU_INTERACTIVE_BOOST
+	bool "Infinity CPU Touch Boost (Interactive Governor)"
+	depends on CPU_FREQ_GOV_INTERACTIVE
+	default y if INFINITY_KERNEL && CPU_FREQ_GOV_INTERACTIVE
+	help
+	  Touch-triggered CPU frequency boost for reduced input latency.
 
 config CPU_FREQ_GOV_PERFORMANCE
 	tristate "'performance' governor"
