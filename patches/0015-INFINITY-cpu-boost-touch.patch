diff --git a/drivers/cpufreq/cpufreq_interactive.c b/drivers/cpufreq/cpufreq_interactive.c
--- a/drivers/cpufreq/cpufreq_interactive.c
+++ b/drivers/cpufreq/cpufreq_interactive.c
@@ -145,7 +145,15 @@
 static spinlock_t speedchange_cpumask_lock;
 
 /* Target load. Lower values result in higher CPU speeds. */
 #define DEFAULT_TARGET_LOAD 90
+#ifdef CONFIG_INFINITY_CPU_INTERACTIVE_BOOST
+static unsigned int infinity_touch_boost_ms = 500;
+module_param_named(touch_boost_ms, infinity_touch_boost_ms, uint, 0644);
+MODULE_PARM_DESC(touch_boost_ms, "Touch boost duration in ms (default: 500)");
+static bool infinity_touch_boost_enabled = true;
+module_param_named(touch_boost_enabled, infinity_touch_boost_enabled, bool, 0644);
+MODULE_PARM_DESC(touch_boost_enabled, "Enable touch boost (default: true)");
+#endif
 static unsigned int default_target_loads[] = {DEFAULT_TARGET_LOAD};
 
 #define DEFAULT_SAMPLING_RATE (20 * USEC_PER_MSEC)
