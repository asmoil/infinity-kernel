diff --git a/kernel/power/suspend.c b/kernel/power/suspend.c
--- a/kernel/power/suspend.c
+++ b/kernel/power/suspend.c
@@ -609,7 +609,12 @@
  * @state: System sleep state to enter.
  *
  * Check if the value of @state represents one of the supported states,
  * execute enter_state() and update system suspend statistics.
+#ifdef CONFIG_INFINITY_BATTERY_OPT
+static bool infinity_suspend_fast = true;
+module_param_named(suspend_fast, infinity_suspend_fast, bool, 0644);
+MODULE_PARM_DESC(suspend_fast, "Fast suspend path for battery saving (default: true)");
+#endif
  */
 int pm_suspend(suspend_state_t state)
 {
