diff --git a/kernel/power/wakelock.c b/kernel/power/wakelock.c
--- a/kernel/power/wakelock.c
+++ b/kernel/power/wakelock.c
@@ -20,7 +20,12 @@
 #include <linux/slab.h>
 #include <linux/workqueue.h>
 
 #include "power.h"
+#ifdef CONFIG_INFINITY_BATTERY_OPT
+static unsigned int infinity_wakelock_expire_ms = 5000;
+module_param_named(wakelock_expire_ms, infinity_wakelock_expire_ms, uint, 0644);
+MODULE_PARM_DESC(wakelock_expire_ms, "Auto-expire inactive wakelocks (0=disabled)");
+#endif
 
 static DEFINE_MUTEX(wakelocks_lock);
 
